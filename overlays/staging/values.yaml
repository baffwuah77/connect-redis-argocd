# Staging Redis Configuration
replicas: 2

resources:
  limits:
    cpu: 1000m
    memory: 1Gi
  requests:
    cpu: 250m
    memory: 256Mi

# Enable persistence
persistence:
  enabled: true
  size: 5Gi
  storageClass: standard

# Enable auth
auth:
  enabled: true
  existingSecret: redis-secret
